pipeline {
    agent any

    stages {
    
        stage('Clear') {
            steps { 
               echo 'hello'
               bat ''' set limit = 10
               for /f %%A in ('dir "D:\\JenkinsHome\\workspace\\PipelineOne\\[REALEASE]" /a-d-s-h /b ^| find /v /c ""') do set a=%%A
               set File_count = %a%
                if File_count GTR limit delete * '''
            }
        }
        stage('Build') {
            steps {
                echo 'Building'
                bat 'build.bat'  
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying'
                bat 'deploy.bat'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing '
                bat 'pipelinetest.bat'
            }
        }
        stage('Release') {
            steps {
                echo 'Releasing'
                 bat 'release.bat'
            }
        }
    }
}
